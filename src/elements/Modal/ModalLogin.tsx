import Modal from "./Modal";

export default function ModalLogin(active: unknown, setActive: unknown) {
  return (
    <Modal active={active} setActive={setActive}>
      <p>123</p>
    </Modal>
  );
}
